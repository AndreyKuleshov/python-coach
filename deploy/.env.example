# === Postgres (local dev via docker-compose) ===
# Must stay consistent with services/api/.env DATABASE_URL.
POSTGRES_USER=coach
POSTGRES_PASSWORD=<replace_me>
POSTGRES_DB=coach
# 5433 avoids clashing with a host Postgres already on 5432.
POSTGRES_PORT=5544
